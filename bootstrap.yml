spring:
  jpa:
    hibernate.ddl-auto: update
  datasource:
    initialization-mode: always
    platform: postgres
    url: jdbc:postgresql://localhost:5433/reiseportal
    username: postgres
    password: secret
